// 화살표 함수로 함수 생성

// const add = (x, y) => {
//     return x + y;
// };

const add = (a, b) => {
    return a + b;
};

console.log(add(2, 5));

const sayHello = () => {
    return console.log("hello");
};

sayHello();
